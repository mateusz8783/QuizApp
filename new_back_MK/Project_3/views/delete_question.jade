//- extends layout

//- block content
//-  h2.page-header Delete Question
//-  p Fill out all of the required fields to delete question
//-  if errors
//-   each error, i in errors
//-    div.alert.alert-danger #{error.msg}
//-  form(method='post', action='/delete')
//-   .form-group
//-    label question_id
//-    input.form-control(name='question_id', type='text', placeholder='')
//-    input.btn.btn-primary(type='submit', name='submit',value='Delete')


extends layout

block content
 h2.page-header Delete Question
 p Fill out all of the required fields to delete question
 if errors
  each error, i in errors
   div.alert.alert-danger #{error.msg}
 form(name="/delete", method="post")
    label question_id
    div.input
        span.label Title
        input(type="text", name="question_id")
    div.actions
        input(type="submit", value="Delete")